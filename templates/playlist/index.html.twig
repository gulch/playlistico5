{% extends 'base.html.twig' %}

{% block title %}Playlist index{% endblock %}

{% block body %}
    {% include('_partials/nav.html.twig') %}

    <div class="container is-widescreen">

        <p>&nbsp;</p>

        <h1 class="title is-1">
            Playlists
        </h1>

        <a href="{{ path('route_playlist_new') }}" class="button">
            <span class="icon is-small">
              <i class="fas fa-plus"></i>
            </span>
            <span>Create new</span>
        </a>

        <p>&nbsp;</p>

        <table class="table is-fullwidth is-striped">
            <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Create Date</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for playlist in playlists %}
                <tr>
                    <td>{{ playlist.id }}</td>
                    <td>
                        <strong>
                            <a href="{{ path('route_playlist_show', {'id': playlist.id}) }}">
                                {{ playlist.title }}
                            </a>
                        </strong>
                    </td>
                    <td>{{ playlist.createdAt ? playlist.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                    <td>
                        <div class="buttons has-addons">
                            <a href="{{ path('route_playlist_show', {'id': playlist.id}) }}"
                               class="button"
                               title="Show"
                            >
                                <span class="icon is-small">
                                    <i class="fas fa-eye"></i>
                                </span>
                            </a>
                            <a href="{{ path('route_playlist_edit', {'id': playlist.id}) }}"
                               title="Edit"
                               class="button"
                            >
                                <span class="icon is-small">
                                    <i class="fas fa-marker"></i>
                                </span>
                            </a>
                        </div>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

    </div>

{% endblock %}